name: KiCad

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: write

jobs:
  schematic:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Workflow syntax smoke test
        run: echo "KiCad workflow syntax OK"
